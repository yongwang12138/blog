# Codex 协作规则（博客项目）

本文件用于约束 AI/Codex 在本仓库内的修改行为。

## 1. hugo.toml 注释保护（强制）

1. 不得删除、改写、合并已有中文注释。
2. 修改配置项时，必须保留该配置项上方/同区块的中文注释。
3. 新增配置项时，必须补充中文注释，说明用途与可选值。
4. 禁止整文件重写 `hugo.toml`，只允许最小范围增量修改。
5. 如果编码异常，先修复编码，再原样恢复中文注释后再改配置。

## 2. Markdown 文件命名（强制）

1. `content/` 下新建的 `.md` 文件名必须使用英文（小写 + 连字符），不得使用中文文件名。
2. 示例：`writing-style-cheatsheet.md`、`git-basic-guide.md`。
3. 页面显示中文请放在 front matter 的 `title` 字段，不要放在文件名里。

## 3. 修改 hugo.toml 的执行方式（强制）

1. 优先做精确行级修改，不做大段覆盖。
2. 修改后必须检查：
   - 中文注释是否仍在；
   - TOML 语法是否完整（引号、换行、数组/表结构）。
3. 修改后必须运行一次构建验证（例如 `hugo -D`）。

## 4. 输出要求

1. 每次改 `hugo.toml`，在回复中明确说明：
   - 改了哪些键；
   - 哪些中文注释被保留；
   - 是否通过构建验证。
2. 如果发现注释丢失，必须先恢复注释，再进行其他改动。

---

如果用户指令与本文件冲突：
优先满足“保留中文注释”和“最小范围修改”。